setTimeout(function(){
  alert("This is the alert using Jquery with a delay of 5 secs. In the case of Jquery as .ready() is getting deprecated you just have to put the handler which will be executed once the DOM is loaded.");
}, 5000);